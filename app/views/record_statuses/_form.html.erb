<%= form_with(model: record_status) do |form| %>
  <% if record_status.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(record_status.errors.count, "error") %> prohibited this record_status from being saved:</h2>

      <ul>
        <% record_status.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :code, style: "display: block" %>
    <%= form.text_field :code %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :about, style: "display: block" %>
    <%= form.text_area :about %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
